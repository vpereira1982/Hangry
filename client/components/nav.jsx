const React = require('react');

const Nav = () => (
  <header>
    <nav className="navbar fixed-top navbar-expand-lg navbar-dark bg-indigo">
      <a className="navbar-brand" href="#"><strong>Hangry Home</strong></a>
    </nav>
  </header>
);

module.exports = Nav;
